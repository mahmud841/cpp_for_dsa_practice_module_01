#include <iostream>
using namespace std;
int main()
{
    int a, j;
    // while (scanf("%d %d", &a, &j) != EOF)
    while (cin>>a>>j)
    {
        // printf("%d %d\n", a, j);
        cout << a << " " << j << endl;
    }
    return 0;
}